{% import '@pcc-components/component.twig' as component %}
{% do attach_library('pcc/<component>') %}

<div {{ component.attributes('c-<component>', modifiers, attr) }}>
  {{ content }}
</div>


{#Include this snippet in the drupal twig template.#}
{#{%#}
{#  include '@pcc-components/<component>/<component>.twig' with {#}
{#    attr: attributes.addClass(classes),#}
{#    content: content,#}
{#  } only#}
{#%}#}
